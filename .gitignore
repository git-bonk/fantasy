# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo

# Data (generated)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Env / secrets
.env
.env.local
.env.*.local
overrides.json

# Logs & misc
*.log
.DS_Store

# Deployment (host-specific overrides & tunnel credentials)
docker-compose.override.yml
.cloudflared/
cloudflared-*.json
cloudflared.deb

# TLS material (never commit private keys/certs)
*.pem
*.key

# Working docs (kept locally, not tracked)
docs/

# graphify code-graph output (generated)
graphify-out/
